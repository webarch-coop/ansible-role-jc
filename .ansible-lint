---
# https://ansible-lint.readthedocs.io/rules/
skip_list:
  # You can improve the task key order to: name, tags, block (warning)
  - key-order[task]
  # The metadata schema validation fails as it expect the versions of Debian to be intergers and not strings
  - schema[meta]
# vim: syntax=yaml
...
