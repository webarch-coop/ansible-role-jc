---
# https://docs.ansible.com/ansible-lint/rules/default_rules.html
warn_list:
  # Computed fully qualified role name of jc does not follow current galaxy requirements
  - role-name
skip_list:
  # Lines should be no longer than 160 chars
  - "204"
# vim: syntax=yaml
...
